[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ahl"
version = "0.1.0"
description = "A very simple CLI tool"
requires-python = ">=3.10"

[project.scripts]
ahl = "ahl.cli:main"
