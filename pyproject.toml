[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ahl"
version = "0.2.0"
description = "AMD Hackathon Launcher — manage remote server, tunnel, model downloads"
requires-python = ">=3.10"

[project.scripts]
ahl = "ahl.cli:main"
